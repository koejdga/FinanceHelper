import { View } from "react-native";

const Separator = () => {
  return (
    <View
      style={{
        borderBottomWidth: 1,
        height: 5,
        borderColor: "rgba(0, 0, 0, 0.5)",
      }}
    ></View>
  );
};

export default Separator;
